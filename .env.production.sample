# Casino-Club F2P - Production environment sample
# Copy to .env.production and adjust values before building/deploying.

# Backend API endpoints
NEXT_PUBLIC_API_BASE=https://api.example.com
NEXT_PUBLIC_API_ORIGIN=https://api.example.com

# Realtime
NEXT_PUBLIC_REALTIME_ENABLED=1

# Optional: disable any dev-only mocks explicitly (default: off in prod anyway)
NEXT_PUBLIC_ENABLE_MOCKS=0
