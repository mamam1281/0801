services:
  backend:
    environment:
      ENABLE_LEGACY_GAMES_WS: "0"
    ports:
      - "8001:8000"
  frontend:
    environment:
      NODE_OPTIONS: "--max-old-space-size=2048"
    ports:
      - "3001:3000"
  postgres:
    ports:
      - "5433:5432"
