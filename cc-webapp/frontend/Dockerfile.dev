FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
# Use legacy peer deps to tolerate React 19 + older peer ranges (e.g., next-themes)
RUN npm install --legacy-peer-deps

COPY . .

CMD ["npm", "run", "dev"]
