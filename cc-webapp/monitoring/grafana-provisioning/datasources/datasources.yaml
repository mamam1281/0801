apiVersion: 1

# Provision a Prometheus datasource pointing to the Prometheus service on the ccnet network

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    version: 1
    editable: false
