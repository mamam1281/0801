# This file is intentionally left blank to avoid duplicate datasource provisioning.
# Canonical Prometheus datasource is defined in 'datasources.yaml' in this folder.
# Keeping only comments ensures Grafana ignores this file safely.
