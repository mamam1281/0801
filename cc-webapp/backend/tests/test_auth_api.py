import pytest
pytest.skip("Legacy auth API script skipped – refactor pending", allow_module_level=True)

"""(SKIPPED) Legacy procedural auth test removed. Future replacement will use TestClient fixtures."""

# File intentionally left empty (all legacy code stripped).
